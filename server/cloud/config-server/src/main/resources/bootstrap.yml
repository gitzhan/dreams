info:
  version: 1.0.0-SNAPSHOT

server: 
  port: 8999
  
spring:
  profiles:
    active: dev
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/gitzhan/dreams/
          searchPaths: config
#           公开仓库可以不用写用户名和密码
#          username:
#          password:

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/

