info:
  version: 1.0.0-SNAPSHOT

server: 
  port: 8761
  
spring:
  profiles:
    active: docker
  application:
    name: pro-zuul
  cloud:
    config:
      label: master
#      profile: dev
      discovery:
        enabled: true
        service-id: config-server
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://server-master:8760/eureka/

