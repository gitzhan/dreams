info:
  version: 1.0.0-SNAPSHOT

server: 
  port: 8761
  
spring:
  profiles:
    active: dev
  application:
    name: pro-zuul
  cloud:
    config:
      label: master
#      profile: dev
      discovery:
        enabled: true
        service-id: config-server

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/
      
